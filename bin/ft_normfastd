#!/bin/zsh

if [ "$#" -ne 1 ]; then
  echo "Usage: $0 <source_directory>"
  exit 1
fi

find "$1" -type f -print0 | while read -d $'\0' file; do
  echo ".................................."
  echo ". Converting '$file'"
  echo ".................................."
  ft_normfast "$file"
done

